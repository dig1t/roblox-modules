[package]
name = "dig1t/dlib"
version = "1.1.8"
license = "Apache-2.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

include = ["src", "src/**", "wally.toml", "default.project.json"]
exclude = ["**"]

[dependencies]
